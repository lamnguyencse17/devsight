import React from 'react'
import {NextPage} from 'next';

const Policy: NextPage = () => {
    return <div>
        <h2>Privacy Policy</h2>
        <p>This site uses JSON Web Tokens</p>

        <p>Data provided to this site is exclusively used to support signing in and is not
            passed to any third party services, other than via SMTP or OAuth
            for the purposes of authentication.</p>
    </div>
}

export default Policy
